function vowels(ch){
   ch= ch.toLowerCase();
    if(ch==="a"||ch==="e"||ch==="i"||ch==="o"||ch==="u"){
        console.log("vowels")
    }
    else{
        console.log("consonants")
    }
}
vowels("b")
vowels("i")
vowels("A")